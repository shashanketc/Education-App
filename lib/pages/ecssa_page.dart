import 'package:flutter/material.dart';

class ECSSAPage extends StatelessWidget {
  const ECSSAPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("ECSSA"),
      ),
    );
  }
}
